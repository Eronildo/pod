name: flutter_pod_example
description: A new Flutter project.

publish_to: "none"

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_pod:
    path: ../../flutter_pod
  pod:
    path: ../../pod

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  flutter_pod:
    path: ../../flutter_pod
  pod:
    path: ../../pod

flutter:
  uses-material-design: true